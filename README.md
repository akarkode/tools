# JSON & Base64 Tools 🧰

A simple yet elegant web-based tool for beautifying JSON and encoding/decoding Base64. Built with vanilla HTML, CSS, and JavaScript — no frameworks, no build tools, just open and use.

---

## ✨ Features

- **JSON Beautifier**
  - Fixes single quotes (`'`) to double quotes (`"`)
  - Converts `None` to `null`
  - Fills empty values with `null` (e.g., `{ "key": }` → `{ "key": null }`)
  - Auto-indents and formats JSON
  - Paste, Copy, and Export to `.json`

- **Base64 Encode/Decode**
  - Encode text to Base64
  - Decode Base64 to text
  - Paste, Copy, and Export to `.txt`

- **Modern UI**
  - Clean, responsive layout
  - Sidebar navigation
  - Smooth textarea resizing
  - Sticky action bar for easy access to buttons

---